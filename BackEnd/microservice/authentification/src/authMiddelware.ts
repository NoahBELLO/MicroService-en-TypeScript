import { Request, Response, NextFunction } from "express";

// code pour vérifier les tokens
const authMiddleware: Function = async (req: Request, res: Response, next: NextFunction) => { }

export default authMiddleware